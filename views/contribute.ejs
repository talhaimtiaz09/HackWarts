<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="./css/output.css">
    <title>Contribute</title>
</head>
<body>
    <nav class="flex justify-between text-teal-500 bg-white">
        <span class="m-4 font-bold">Logo</span>
        <ul class="flex">
            <li class="my-4 mx-6"><a href="/">Home</a></li>
            <li class="my-4 mx-6"><a href="/courses">Course List</a></li>
            <li class="my-4 mx-6"><a href="/aboutUs">About</a></li>
        </ul>
      </nav>
      <form  class="bg-teal-100 text-xl font-semibold opacity-50 flex flex-col w-1/3 mx-auto p-10 mt-32"  action="/contributing" enctype="multipart/form-data" method="post">
        <span class="text-teal-500 font-bold text-center text-2xl py-4">Contribute</span>
        <label class="my-4">Course *(Add link to form of unavailable courses)</label>
        <select name="course" id="course">
            <% for(let i = 0; i < courses.length; i++){ %>
                <option value="<%= courses[i].courseCode %>"><%= courses[i].courseCode %></option>
            <% } %>
        </select>
        <label class="my-4">Category</label>
        <select name="category" id="category">
            <option value="Quizzes">Quizzes</option>
            <option value="MidsFinals">Mid or Final</option>
            <option value="Assignments">Assignments</option>
        </select>
        <label class="my-4">File Name:</label>
        <input type="text" name="filename">
        <label class="my-4">Upload file (only PDF*)</label>
        <input type="file" accept=".pdf" name="file">
        <button class="block mx-auto my-4 px-4 py-1 active:border-teal-600 active:drop-shadow-none drop-shadow-lg bg-white text-teal-500 border-2 border-teal-500">Submit</button>
      </form>
    
</body>
</html>